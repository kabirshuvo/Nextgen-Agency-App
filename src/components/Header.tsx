import React from "react";
import ThemeToggler from "./ThemeToggler";

const Header = () => {
  return (
    <section>
      <div>
        <ThemeToggler />
      </div>
    </section>
  );
};

export default Header;
